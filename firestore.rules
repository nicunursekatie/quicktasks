rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Only allow access if the user is logged in
      // For maximum security, replace 'null' with your specific UID later
      allow read, write: if request.auth != null;
    }
  }
}